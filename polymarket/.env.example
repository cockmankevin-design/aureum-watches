# Polymarket US API credentials.
# Create an API key in your Polymarket US account and paste the values here,
# then copy this file to `.env` (which is gitignored — never commit real keys).
POLYMARKET_KEY_ID=your-key-id-uuid
POLYMARKET_SECRET_KEY=your-base64-ed25519-secret-key
