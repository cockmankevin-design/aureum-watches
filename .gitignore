# PRIVATE — never publish supplier costs / margins
SOURCING.md

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.log

# private until domain cutover
DOMAIN-CUTOVER.md

# local dev-only GitHub Pages emulator (SPA 404 fallback testing)
_ghpreview.py

# private marketing content (internal working files, not for the public repo)
marketing/
